/** Messaging mutation response schema exposed by Claw Router. */
export interface MessagingMutationResponse {
  /** Id field on messaging mutation response. */
  id: string;
  /** Status field on messaging mutation response. */
  status: string;
}
