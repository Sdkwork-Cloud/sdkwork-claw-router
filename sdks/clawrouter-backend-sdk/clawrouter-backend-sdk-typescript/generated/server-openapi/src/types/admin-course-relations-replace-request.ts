/** Admin course relations replace request schema exposed by Claw Router. */
export interface AdminCourseRelationsReplaceRequest {
  /** Items field on admin course relations replace request. */
  items: Record<string, unknown>[];
}
