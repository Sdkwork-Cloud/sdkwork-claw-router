import type { AdminCourseLessonItem } from './admin-course-lesson-item';

/** Admin course lesson mutation response schema exposed by Claw Router. */
export interface AdminCourseLessonMutationResponse {
  /** Item field on admin course lesson mutation response. */
  item: AdminCourseLessonItem;
}
