import type { AdminCourseSectionItem } from './admin-course-section-item';

/** Admin course section mutation response schema exposed by Claw Router. */
export interface AdminCourseSectionMutationResponse {
  /** Item field on admin course section mutation response. */
  item: AdminCourseSectionItem;
}
