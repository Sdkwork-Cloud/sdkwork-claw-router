import type { OpenPlatformPayBindingItem } from './open-platform-pay-binding-item';

/** Open platform pay binding list response schema exposed by Claw Router. */
export interface OpenPlatformPayBindingListResponse {
  /** Items field on open platform pay binding list response. */
  items: OpenPlatformPayBindingItem[];
}
