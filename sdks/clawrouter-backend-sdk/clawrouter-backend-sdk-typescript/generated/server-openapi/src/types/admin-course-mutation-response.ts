import type { AdminCourseItem } from './admin-course-item';

/** Admin course mutation response schema exposed by Claw Router. */
export interface AdminCourseMutationResponse {
  /** Item field on admin course mutation response. */
  item: AdminCourseItem;
}
