import type { AdminCourseDashboard } from './admin-course-dashboard';

/** Admin course dashboard response schema exposed by Claw Router. */
export interface AdminCourseDashboardResponse {
  /** Item field on admin course dashboard response. */
  item: AdminCourseDashboard;
}
