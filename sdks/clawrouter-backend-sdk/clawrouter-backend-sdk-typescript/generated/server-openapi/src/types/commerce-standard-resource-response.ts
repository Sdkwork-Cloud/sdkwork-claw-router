/** Commerce standard resource response schema exposed by Claw Router. */
export interface CommerceStandardResourceResponse {
  /** Item field on commerce standard resource response. */
  item: Record<string, unknown>;
}
