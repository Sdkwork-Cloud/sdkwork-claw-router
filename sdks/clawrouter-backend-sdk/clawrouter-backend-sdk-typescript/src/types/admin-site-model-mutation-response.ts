import type { AdminSiteModelItem } from './admin-site-model-item';

/** Admin site model mutation response schema exposed by Claw Router. */
export interface AdminSiteModelMutationResponse {
  /** Item field on admin site model mutation response. */
  item: AdminSiteModelItem;
}
