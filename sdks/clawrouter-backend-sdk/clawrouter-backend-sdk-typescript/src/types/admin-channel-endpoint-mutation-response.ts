import type { AdminChannelEndpointItem } from './admin-channel-endpoint-item';

/** Admin channel endpoint mutation response schema exposed by Claw Router. */
export interface AdminChannelEndpointMutationResponse {
  /** Item field on admin channel endpoint mutation response. */
  item: AdminChannelEndpointItem;
}
