import type { AdminAccessGroupChannelBindingItem } from './admin-access-group-channel-binding-item';

/** Admin access group channel bindings response schema exposed by Claw Router. */
export interface AdminAccessGroupChannelBindingsResponse {
  /** Items field on admin access group channel bindings response. */
  items: AdminAccessGroupChannelBindingItem[];
}
