import type { CommerceExchangeRuleItem } from './commerce-exchange-rule-item';

/** Admin exchange rule mutation response schema exposed by Claw Router. */
export interface AdminExchangeRuleMutationResponse {
  /** Item field on admin exchange rule mutation response. */
  item: CommerceExchangeRuleItem;
}
