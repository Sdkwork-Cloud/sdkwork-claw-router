import type { AdminAccessGroupChannelBindingInput } from './admin-access-group-channel-binding-input';

/** Admin access group channel bindings replace request schema exposed by Claw Router. */
export interface AdminAccessGroupChannelBindingsReplaceRequest {
  /** Items field on admin access group channel bindings replace request. */
  items: AdminAccessGroupChannelBindingInput[];
}
