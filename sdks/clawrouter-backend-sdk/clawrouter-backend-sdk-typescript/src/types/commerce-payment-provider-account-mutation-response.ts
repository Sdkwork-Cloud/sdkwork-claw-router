import type { CommercePaymentProviderAccountItem } from './commerce-payment-provider-account-item';

/** Commerce payment provider account mutation response schema exposed by Claw Router. */
export interface CommercePaymentProviderAccountMutationResponse {
  /** Item field on commerce payment provider account mutation response. */
  item: CommercePaymentProviderAccountItem;
}
