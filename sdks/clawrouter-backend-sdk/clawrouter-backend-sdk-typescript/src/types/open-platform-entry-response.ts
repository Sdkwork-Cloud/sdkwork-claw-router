import type { OpenPlatformEntryItem } from './open-platform-entry-item';

/** Open platform entry response schema exposed by Claw Router. */
export interface OpenPlatformEntryResponse {
  /** Item field on open platform entry response. */
  item: OpenPlatformEntryItem;
}
