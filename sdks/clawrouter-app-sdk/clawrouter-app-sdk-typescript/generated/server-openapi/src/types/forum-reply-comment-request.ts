/** Forum reply comment request schema exposed by Claw Router. */
export interface ForumReplyCommentRequest {
  /** Content field on forum reply comment request. */
  content: string;
  /** Device info field on forum reply comment request. */
  deviceInfo?: string;
}
