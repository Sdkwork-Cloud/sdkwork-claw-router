/** Iam oauth authorization url response schema exposed by Claw Router. */
export interface IamOauthAuthorizationUrlResponse {
  /** Auth url field on iam oauth authorization url response. */
  authUrl: string;
}
