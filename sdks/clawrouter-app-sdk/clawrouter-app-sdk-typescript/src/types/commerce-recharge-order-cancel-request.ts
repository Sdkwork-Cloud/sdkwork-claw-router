/** Commerce recharge order cancel request schema exposed by Claw Router. */
export interface CommerceRechargeOrderCancelRequest {
  /** Reason field on commerce recharge order cancel request. */
  reason?: string;
}
