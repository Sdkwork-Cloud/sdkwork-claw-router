/** Update routing strategy response schema exposed by Claw Router. */
export interface UpdateRoutingStrategyResponse {
  /** Success field on update routing strategy response. */
  success: boolean;
}
