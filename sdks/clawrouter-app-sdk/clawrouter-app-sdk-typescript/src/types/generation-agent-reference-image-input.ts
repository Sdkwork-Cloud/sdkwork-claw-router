/** Generation agent reference image input schema exposed by Claw Router. */
export interface GenerationAgentReferenceImageInput {
  /** Asset id field on generation agent reference image input. */
  assetId?: string;
  /** Data url field on generation agent reference image input. */
  dataUrl?: string;
  /** Mime type field on generation agent reference image input. */
  mimeType?: string;
  /** Name field on generation agent reference image input. */
  name: string;
  /** Size bytes field on generation agent reference image input. */
  sizeBytes?: number;
  /** Url field on generation agent reference image input. */
  url?: string;
}
