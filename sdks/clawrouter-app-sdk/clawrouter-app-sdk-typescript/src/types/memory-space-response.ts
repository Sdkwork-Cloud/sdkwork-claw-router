import type { MemorySpaceItem } from './memory-space-item';

/** Memory space response schema exposed by Claw Router. */
export interface MemorySpaceResponse {
  /** Item field on memory space response. */
  item: MemorySpaceItem;
}
