export interface PlusFeedsRecord {
  author?: Record<string, unknown>;
  cover_images?: Record<string, unknown>;
  publish_time?: string;
  resource_list?: Record<string, unknown>;
  source?: string;
  source_url?: string;
  summary?: string;
  tags?: Record<string, unknown>;
  user_id?: string;
}
