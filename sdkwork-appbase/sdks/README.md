# SDKWork Appbase SDKs

This directory hosts reusable SDK-facing integration packages for `sdkwork-appbase`.

- `sdkwork-sdk-downloader/`: TypeScript orchestration, caching, packaging, and retention for generated SDK artifacts
