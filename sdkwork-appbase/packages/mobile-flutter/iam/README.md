# mobile-flutter/iam

Reserved for Flutter IAM integration packages.

Canonical IAM contracts live under `packages/common/iam`; Flutter packages must preserve the same API, database, SDK, token, and context semantics.
