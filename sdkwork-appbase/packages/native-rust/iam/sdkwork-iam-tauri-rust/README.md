# sdkwork_iam_tauri

Tauri host adapter contract for SDKWork IAM local/private deployments.

This crate exposes the adapter manifest and route catalog. It does not own IAM business logic, SQL persistence, or API contract definitions.
