[package]
name = "sdkwork_iam_core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SDKWork canonical IAM domain, context, and dual-token contracts for local/private Rust deployments."

[lib]
path = "src/lib.rs"
