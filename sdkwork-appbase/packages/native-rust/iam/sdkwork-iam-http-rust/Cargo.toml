[package]
name = "sdkwork_iam_http"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SDKWork canonical IAM HTTP route contracts for Rust local/private deployments."

[lib]
path = "src/lib.rs"

[dependencies]
sdkwork_iam_core = { path = "../sdkwork-iam-core-rust" }
