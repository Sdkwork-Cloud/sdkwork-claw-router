[package]
name = "sdkwork_rpc_core"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "SDKWork RPC metadata, error mapping, and service manifest primitives."

[lib]
path = "src/lib.rs"

[dependencies]
tonic = "0.14"
prost-types = "0.14"
