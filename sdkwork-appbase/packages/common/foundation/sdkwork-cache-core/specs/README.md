# Cache Core Specs

This package defines the SDKWork cache framework contracts and runtime rules.

The component manifest declares the public cache contracts, provider kinds,
deployment binding, admin management operations, and verification commands.
Application-specific cache implementations must follow this local spec plus the
root SDKWork standards linked from `component.spec.json`.
