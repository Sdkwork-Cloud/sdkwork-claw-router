{
  "name": "@sdkwork/platform",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "SDKWork provider-neutral platform module for accounts, entries, hooks, messages, menus, notices, payment bindings, outbox, and generated SDK ports.",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "default": "./src/index.ts"
    }
  },
  "files": [
    "src",
    "README.md"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "pnpm --dir ../../../.. exec vitest run packages/common/integration/sdkwork-platform/tests --config vitest.config.ts --configLoader native --pool vmThreads"
  },
  "dependencies": {
    "@sdkwork/conversation": "workspace:*"
  },
  "sdkwork": {
    "architecture": "common",
    "capability": "platform",
    "domain": "integration",
    "packageType": "domain",
    "status": "standard"
  }
}
