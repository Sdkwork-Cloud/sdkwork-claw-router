# mobile-react/iam

Reserved for mobile React IAM integration packages.

Canonical IAM contracts live under `packages/common/iam`; mobile React packages must depend on those shared contracts rather than redefining auth, tenant, user, role, or permission models.
