# SDKWork Claw Router Admin Analytics

This package owns the admin statistics workspace. It reads the backend analytics overview through the generated `@sdkwork/clawrouter-backend-sdk` system analytics surface.
