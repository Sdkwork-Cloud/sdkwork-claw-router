# SDKWork File Platform PC React

Aggregate PC React exports for SDKWork file platform building blocks.

Use this package when an application wants one stable integration entrypoint for
upload, picker, attachment, preview, drive, and storage usage components. It
contains no business logic and does not call APIs directly.
