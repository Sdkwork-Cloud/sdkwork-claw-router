# SDKWork File Attachments PC React

Reusable attachment list and manager blocks for slot-based business file
bindings.

Business pages provide `slotCode` and target object. Components load and remove
bindings through `@sdkwork/file-service` and only render stable `FileRef`
metadata.
